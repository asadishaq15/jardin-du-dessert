/** Single value for drei `<Html distanceFactor>` on terrain props — consistent chip scale at comparable depth. */
export const HOVER_HTML_DISTANCE_FACTOR = 12

/** Default multiplier for stacked chip inner text sizes in `HoverMesh`. */
export const HOVER_STACKED_FONT_SCALE = 1

/** Sun/moon disks are large in frame; slightly smaller chip type than terrain. */
export const HOVER_CELESTIAL_STACKED_FONT_SCALE = 1
